<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pokeball_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pokeball_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto01"
        tools:targetApi="31">
        <activity
            android:name=".ui.camara.view.Camara"
            android:exported="true" />
        <activity
            android:name=".ui.detalle.view.Detalle"
            android:exported="true" />
        <activity
            android:name=".ui.equipo.view.Equipo"
            android:exported="true" />
        <activity
            android:name=".ui.lista.view.Lista"
            android:exported="true" />
        <activity
            android:name=".ui.usuario.view.Usuario"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Proyecto01">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>